
class version(object):

    def add_version(version_name, dir, executable, folder_size, in_place):
        if not version.__version_exists(version_name):
            print("test")

    def __version_exists(version_name):
        print("check if version exists")
